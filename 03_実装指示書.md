# 楽食ふじた 公式WEBサイト 実装指示書

| 項目 | 内容 |
|---|---|
| 文書名 | 楽食ふじた 公式WEBサイト 実装指示書 |
| 版数 | v1.0 |
| 作成日 | 2026-08-04 |
| 作成者 | 梅田 |
| 対応文書 | 要件定義書 v1.0／設計書 v1.0 |
| 基準実装 | `fujita-prototype-v2.html` |

---

## 0. この文書の使い方

- 実装者は人間のエンジニアでも、AIコーディングエージェント（Claude Code等）でもよい。§6にそのまま渡せる依頼プロンプト例を用意した
- **正の優先順位**: 見た目・演出の挙動は `fujita-prototype-v2.html` を正とする。仕様値・判断基準は設計書を正とする。矛盾を見つけた場合は実装者の判断で直さず、報告して文書側を改版する
- **画像素材は届いていない前提で作業する**。画像がない状態でプレースホルダが正しく表示されれば検収可能（要件FR-08）

---

## 1. フェーズ計画

| フェーズ | 内容 | 状態 |
|---|---|---|
| Phase 1 | プロトタイプの本番化と静的サイト公開（本書§2） | **今回の作業範囲** |
| Phase 2 | Astro移行＋microCMS連携（お品書き・お知らせを店主がスマホ更新）（本書§3） | 次期 |
| Phase 3 | 自社予約システム導入 | 保留（予約APIの費用都合。当面は電話＋外部予約リンクで運用） |

---

## 2. Phase 1: 静的公開の作業手順

### 2.1 リポジトリ初期化

1. Gitリポジトリを作成（例: `rakushoku-fujita-web`）
2. `fujita-prototype-v2.html` を `index.html` にリネームして配置
3. `images/` フォルダを**空のまま**作成（`.gitkeep` を入れる）
4. ディレクトリ構成は設計書§9に従う

### 2.2 本番化タスク（index.htmlへの追記・修正）

| # | タスク | 内容 |
|---|---|---|
| 1 | タイトル修正 | `<title>` から「（写真ベース プロトタイプ v2）」を削除し「楽食ふじた｜中目黒・日本料理」とする。フッターの「DESIGN PROTOTYPE v2」表記も「© RAKUSHOKU FUJITA」に変更 |
| 2 | favicon | 朱色の落款風「ふ」または屋号1文字のSVGファビコンを作成し `favicon.svg` として設置・`<link>` 追加 |
| 3 | OGP | `og:title` `og:description` `og:type=website` `og:url` `og:image` を `<head>` に追加。OG画像は写真到着まで墨背景＋屋号のプレースホルダ画像（1200×630）を生成して使用 |
| 4 | JSON-LD | §2.5のコードを `<head>` 末尾に追加 |
| 5 | robots / sitemap | `robots.txt`（全許可）と `sitemap.xml`（トップのみ）を作成 |
| 6 | GA4 | 測定タグを導入。加えて予約導線のイベント計測を実装: 電話発信ボタン→ `reserve_tel`、食べログリンク→ `reserve_tabelog`、一休リンク→ `reserve_ikyu`（`gtag('event', ...)` をクリックリスナーで送出） |
| 7 | ENリンク | ヘッダー「EN」はフェーズ2まで非表示にする（要素は残しコメントアウト可） |
| 8 | Instagram | URL確定まではフッターの「INSTAGRAM」を非表示にする |

### 2.3 画像運用ルール（写真到着後の作業）

1. 受領写真を設計書§2（要件§5.1）の縦横比に合わせてトリミング
2. `sharp` または [squoosh.app](https://squoosh.app) でリサイズ・圧縮（JPG品質75目安、長辺は要件の推奨サイズ）
3. 規定のファイル名で `images/` に配置（コード変更不要で反映される）
4. 容量に余裕を持たせたい場合はWebP化し、`<img>` を `<picture>`（WebP＋JPGフォールバック）に置き換えてよい。**その場合も `onerror` によるフォールバック機構は必ず維持すること**
5. `interior-03.jpg`（外観）が用意できない場合: `#info` の写真枠を丸ごとGoogleマップ埋め込み `<iframe>`（住所検索の共有コード）に差し替える。`title` 属性を必ず付与

### 2.4 デプロイ手順

**推奨: Vercel**（Cloudflare Pagesでも同手順で可）

1. GitHub等にリポジトリをpush
2. Vercelで「Add New Project」→ リポジトリを選択 → フレームワーク「Other」（静的）でデプロイ
3. 以後は `main` ブランチへのpushで自動デプロイ
4. 独自ドメイン: 店主確認（要件§8）で決定後、Vercelの「Domains」に追加し、ドメインレジストラ側でDNS設定（A/CNAMEはVercelの指示値）。SSLは自動発行
5. 本公開までは `noindex`（`<meta name="robots" content="noindex">`）を入れておき、公開判断時に外す

### 2.5 JSON-LD（そのまま使用可・確定値に更新すること）

```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Restaurant",
  "name": "楽食ふじた",
  "servesCuisine": "日本料理",
  "address": {
    "@type": "PostalAddress",
    "addressCountry": "JP",
    "addressRegion": "東京都",
    "addressLocality": "目黒区",
    "streetAddress": "中目黒2-0-16 ウィングルビル1F"
  },
  "telephone": "+81-3-6451-2969",
  "url": "https://（ドメイン確定後に記入）",
  "acceptsReservations": "True",
  "priceRange": "￥￥￥"
}
</script>
```

※住所・電話は店主確認後の確定値に必ず差し替える。営業時間が確定したら `openingHoursSpecification` を追加する。

### 2.6 Phase 1 受け入れ基準（検収チェックリスト）

- [ ] 画像なしの状態で全セクションが崩れず表示され、各写真枠にプレースホルダ（推奨カット＋ファイル名）が出る
- [ ] `images/` にダミー画像を規定名で置くと、コード変更なしで反映され、トーン補正がかかる
- [ ] 節気印に当日の節気が表示される（テスト: 端末日付を変更し、8/6→「大暑」・8/7→「立秋」に切り替わること）
- [ ] お品書きの日付が当日の漢数字表記になる
- [ ] 電話ボタンがスマホで発信ダイアログを開く。食べログ・一休リンクが別タブで正しいページを開く
- [ ] ローダー→ヒーロー文字出現→スクロール出現の一連の演出が動作する
- [ ] OSの「視差効果を減らす」設定時に演出が全て無効化され、内容が静的に全表示される
- [ ] キーボードのTab操作でフォーカスリングが見える
- [ ] iPhone Safari／Android Chrome 実機で表示確認（860px・520pxの各レイアウト）
- [ ] Lighthouse（モバイル）: Performance 90以上／Accessibility 95以上／SEO 95以上
- [ ] `noindex` が入った状態でステージングURLが共有できる

---

## 3. Phase 2: Astro移行＋microCMS連携（作業指示）

### 3.1 技術選定

- **Astro 5** を採用（静的コンテンツ中心・高速・View Transitions標準対応）。実装者がNext.jsに習熟している場合はNext.js（App Router・静的生成）でも可。いずれでも見た目はプロトタイプと完全一致させること
- スタイルは現行CSSをそのまま移植してよい（Tailwind化は必須ではない）

### 3.2 コンポーネント分割案

```
src/
├─ layouts/Base.astro        … head共通・粒子・ローダー
├─ components/
│   ├─ Header.astro
│   ├─ Hero.astro            … 節気ロジック含む
│   ├─ Concept.astro
│   ├─ Okonomi.astro         … CMSデータ受け取り
│   ├─ Interior.astro
│   ├─ DayTimeline.astro
│   ├─ Info.astro
│   ├─ Reserve.astro
│   ├─ Footer.astro
│   └─ Photo.astro           … 写真＋フォールバックの共通化
└─ pages/index.astro
```

### 3.3 microCMS スキーマ定義

| API名 | 種別 | フィールド |
|---|---|---|
| `menu`（本日のお品書き） | リスト | `name`（テキスト・品名）／`nameEn`（テキスト・任意）／`photo`（画像・任意）／`featured`（真偽・名物カード表示）／`order`（数値） |
| `news`（お知らせ） | リスト | `title`／`body`（リッチ）／`publishedAt` |
| `info`（店舗情報） | オブジェクト | `hours`（営業時間）／`holiday`（定休日）／`note`（補足） |

- お品書きは `featured: true` の先頭3件を写真カード、残りを「ほかにも」行に表示
- 店主の更新手順書（スマホでのmicroCMS操作・写真アップ手順）を別途1枚作成すること

### 3.4 データ取得・再ビルド

- ビルド時取得（SSG）とし、microCMSのWebhookでVercelのDeploy Hookを叩いて自動再ビルド（更新反映は数分以内）
- APIキーは環境変数で管理し、リポジトリにコミットしない

### 3.5 Phase 2 受け入れ基準

- [ ] microCMSでお品書きを編集→数分以内に本番へ反映される
- [ ] 見た目・演出がプロトタイプ（Phase 1公開版）と一致（目視＋主要画面のスクリーンショット比較）
- [ ] Lighthouse基準はPhase 1と同等以上

---

## 4. 禁止事項・注意事項

1. **店舗情報（住所・電話・営業時間・席数・店主名）を店主確認前に本公開しない**（要件§7-5・§8）
2. 写真プレースホルダが残った状態で `noindex` を外さない（本公開判断は写真投入後）
3. 二十四節気テーブルは近似値である（±1日）。天文計算ライブラリへの置き換えは不要（過剰品質）
4. 朱色（`--shu`）の使用箇所を増やさない（設計書§2.1の原則）
5. Webフォント（Noto Serif JP等）を追加する場合は、サブセット化＋`font-display: swap` を必須とし、LCP悪化がないことをLighthouseで確認する
6. 予約フォーム・問い合わせフォームは実装しない（個人情報の取り扱いを発生させない）
7. 他店・グルメサイトの写真や文章を流用しない（権利上の問題。文言はプロトタイプの初稿か店主提供のものだけを使う）

---

## 5. 引き継ぎ資料一覧

| 資料 | 用途 |
|---|---|
| `01_要件定義書.md` | 何を作るか・検収条件 |
| `02_設計書.md` | トークン・セクション仕様・ロジック仕様 |
| `03_実装指示書.md`（本書） | 作業手順・受け入れ基準 |
| `fujita-prototype-v2.html` | 基準実装（見た目・挙動の正） |

---

## 6. AIコーディングエージェントへの依頼プロンプト例（Claude Code想定）

Phase 1をAIエージェントに依頼する場合、リポジトリに上記4ファイルを置いた上で、以下をそのまま渡してよい。

```
このリポジトリは日本料理店「楽食ふじた」の公式サイトです。
01_要件定義書.md、02_設計書.md、03_実装指示書.md を読み、
fujita-prototype-v2.html を基準実装として、実装指示書の
「2. Phase 1: 静的公開の作業手順」を上から順に実施してください。

制約:
- 見た目・演出は fujita-prototype-v2.html から変えないこと
- 画像素材はまだ無いため、images/ は空のままでよい
  （プレースホルダ表示が正しく出ることが検収条件）
- 実装指示書「4. 禁止事項」を必ず守ること
- 完了したら「2.6 受け入れ基準」のチェックリストを自己検証し、
  結果を報告してください。文書と実装に矛盾を見つけた場合は
  勝手に直さず、報告に含めてください。
```

Phase 2着手時は、§3を対象に同様の形式で依頼する。
