# 楽食ふじた 公式WEBサイト 設計書

| 項目 | 内容 |
|---|---|
| 文書名 | 楽食ふじた 公式WEBサイト 設計書 |
| 版数 | v1.0 |
| 作成日 | 2026-08-04 |
| 作成者 | 梅田 |
| 対応文書 | 要件定義書 v1.0 |
| 基準実装 | `fujita-prototype-v2.html`（見た目・挙動の正。本書と差異がある場合はプロトタイプを正とし、本書を改版する） |

---

## 1. 画面構成

1ページ構成（ワンページ）。固定ヘッダーからページ内アンカーで各セクションへ遷移する。

| 順 | セクション | id | 役割 |
|---|---|---|---|
| 0 | 暖簾ローダー | `#noren` | 初回表示演出（表示後は非表示） |
| 1 | ヘッダー | `#siteHeader` | 屋号・ナビ・「ご予約」常設ボタン（固定表示） |
| 2 | ヒーロー | `#hero` | 店内写真1枚＋縦書きコピー＋二十四節気の朱印 |
| 3 | 想い | `#concept` | コンセプト文＋縦書きサブコピー |
| 4 | 本日のお品書き | `#okonomi` | 日付（漢数字）＋名物3品の写真カード＋その他テキスト |
| 5 | 店内 | `#interior` | 店内写真2枚の段違いレイアウト＋短文 |
| 6 | ふじたの一日 | `#day` | 写真を使わないタイポグラフィの時間軸（4項目） |
| 7 | 店舗情報・アクセス | `#info` | 情報テーブル＋外観写真枠（または地図） |
| 8 | ご予約 | `#reserve` | 電話発信ボタン＋食べログ・一休リンク |
| 9 | フッター | - | 屋号・ナビ・コピーライト |

ナビゲーション項目: お品書き（#okonomi）／店内（#interior）／アクセス（#info）／EN（将来用ダミー）／ご予約（#reserve）

---

## 2. デザイントークン

### 2.1 カラー

| 変数 | 値 | 用途 |
|---|---|---|
| `--sumi` | `#131010` | ベース背景（墨） |
| `--sumi2` | `#1b1714` | 写真枠・浮きの背景 |
| `--sumi3` | `#221d18` | 予備の段差背景 |
| `--ink` | `#eae1d3` | 基本文字色（生成り） |
| `--dim` | `#c6bba9` | 補助文字色 |
| `--mut` | `#857b6e` | 最弱文字色・注記 |
| `--shu` | `#b34a2b` | アクセント（朱）: 予約ボタン・落款 |
| `--shu-soft` | `#d98a68` | 朱の弱トーン: eyebrow・時刻ラベル |
| `--line` | `rgba(234,225,211,.13)` | 罫線（髪の毛ほどの生成り） |

原則: 朱は「予約ボタン」「落款（節気印）」「eyebrow・時刻ラベル」以外に使わない。アクセントの希少性がデザインの要。

### 2.2 タイポグラフィ

| 用途 | 指定 |
|---|---|
| 基本（見出し・本文） | `--serif`: Hiragino Mincho ProN → Yu Mincho → Noto Serif JP → MS PMincho → serif |
| 補助（ラベル・注記・英字） | `--sans`: Hiragino Kaku Gothic ProN → Yu Gothic → Noto Sans JP → -apple-system → sans-serif |
| 文字詰め | `font-feature-settings:"palt"`（プロポーショナルメトリクス） |
| 基本行間・字間 | `line-height: 2` ／ `letter-spacing: .08em` |

| 要素 | サイズ | 字間 |
|---|---|---|
| ヒーロータイトル（縦書き） | `clamp(34px, 5.2vw, 58px)` | `.3em` |
| セクション見出し `.sec-title` | `clamp(24px, 3vw, 32px)` | `.25em` |
| 本文 `.lead` | `15px`（最大行長38em） | 基本値 |
| eyebrow（英字風ラベル） | `11px`（sans） | `.35em` |
| 注記・キャプション | `10〜11px`（sans） | `.12〜.2em` |

フォントはWebフォントを読み込まず、システムフォントのみで構成する（性能優先）。Noto Serif JPをWebフォント導入する場合は実装指示書§5を参照。

### 2.3 面・罫線・角丸

- セクション余白: `padding: 110px 8vw`（860px以下で `80px 6vw`）
- セクション区切り: 上辺に `1px solid var(--line)`
- 角丸: 全要素 `2px`（和の直線的な印象を保つため大きな角丸は使わない）
- フィルム粒子: `body::after` に固定配置したSVGノイズ（feTurbulence）を `opacity: .045` で全面に重ねる

---

## 3. 共通コンポーネント設計

### 3.1 写真コンポーネント `.photo`

すべての写真枠に共通適用。撮影品質のばらつきを吸収する要。

| 項目 | 仕様 |
|---|---|
| 画像 | `object-fit: cover` で枠いっぱいに表示 |
| トーン補正 | `filter: saturate(.8) contrast(1.06) brightness(.9) sepia(.1)`（ヒーローのみ `saturate(.75) brightness(.82) sepia(.12)` とやや強め） |
| 墨オーバーレイ | `::after` で `linear-gradient(180deg, rgba(19,16,16,.1), rgba(19,16,16,.28))` を重ねる（ヒーローは左右＋上下の二重グラデーション。仕様値はプロトタイプ参照） |
| フォールバック | `<img onerror>` で親に `.missing` を付与し画像要素を除去。`.missing` 時は破線枠＋`.ph-label`（推奨カット・ファイル名の案内）を中央表示 |

HTML構造（標準形）:

```html
<figure class="photo">
  <img src="images/dish-01.jpg" alt="のどぐろ塩焼き"
       onerror="this.closest('.photo').classList.add('missing');this.remove()">
  <span class="ph-label">料理写真①（推奨カット説明）<br>images/dish-01.jpg</span>
</figure>
```

### 3.2 ボタン

| クラス | 見た目 | 用途 |
|---|---|---|
| `.btn-reserve` | 朱背景・生成り文字・角丸2px | ヘッダー常設の「ご予約」 |
| `.btn.primary` | 朱背景・大きめ | 予約セクションの電話発信 |
| `.btn` | 罫線のみのゴースト | 食べログ・一休リンク |

ホバー時は朱を `#c85c3a` に明転（`.3s`）。ボタン内の補足（「タップで発信できます」等）は `small` 要素・sans・10px。

### 3.3 見出しセット

`eyebrow`（朱soft・sans・11px・字間.35em）→ `sec-title`（明朝・字間.25em）→ `lead`（補助色・最大38em）の3点セットを全セクションで統一使用。

### 3.4 出現演出 `.reveal`

初期状態 `opacity:0; translateY(24px)` → 付与クラス `.on` で `opacity:1; translateY(0)`（`transition: 1s ease`）。付与条件は§6.4。

---

## 4. セクション別設計

### 4.1 暖簾ローダー `#noren`

- 全画面・墨背景。中央に屋号「楽食ふじた」を縦書き（`clamp(22px,4vw,30px)`・字間.5em）
- 屋号は0.15s遅延で1.1sかけてフェードイン（`norenIn`）
- 解除時は `opacity` 0.9sでフェードアウトし `visibility: hidden`

### 4.2 ヘッダー `#siteHeader`

- `position: fixed`・全幅・`z-index: 50`
- 初期: 透明背景・`padding: 20px 5vw`
- スクロール40px超で `.scrolled`: 背景 `rgba(19,16,16,.86)`＋`backdrop-filter: blur(8px)`、`padding: 14px 5vw`、下辺罫線

### 4.3 ヒーロー `#hero`

- 高さ `100svh`（フォールバック `100vh`）・最小640px
- 写真（`images/hero.jpg`）を全面配置し、`.ready` 付与後に `kenburns`（`scale 1.02→1.12`・24s・ease-out・both）
- コピーは右寄せ・縦書き2列（右: タイトル、左: サブ）。タイトルは1文字ずつ `span.ch` に分割し、0.15s＋0.12s×n の遅延で順次出現（§6.5）
- 左下に節気印（`.sekki-seal`: 朱罫線・縦書き・半透明の墨下地）＋説明キャプション。`.ready` から1.3s遅延でフェードイン
- 右下に SCROLL キュー（1px縦線が2.2s周期で伸縮）
- 文字には `text-shadow` で墨のにじみを付与し、写真上でも可読性を確保

### 4.4 想い `#concept`

2カラムグリッド（左: eyebrow＋見出し＋lead×2、右: 縦書きサブコピー「今日の魚と、今日の酒と。」）。縦書きは `min-height: 300px` で中央配置。

### 4.5 本日のお品書き `#okonomi`

- 見出し行に日付（`#today`・漢数字・JS出力）と注記「※仕入れにより毎日変わります（以下はサンプル表記）」を併記
- 名物3品を `.dish-grid`（`auto-fit minmax(230px,1fr)`・gap `clamp(24px,3vw,44px)`）で表示
- カード: 写真（4:5）＋品名（明朝17px）＋英字キャプション（sans 10px）。ホバーで写真を `scale(1.04)`（1.2s）
- 下部に罫線区切りで「ほかにも」テキスト列、さらに「実装時は、店主がスマホから毎朝ここを更新できます（CMS連携）」の予告文

### 4.6 店内 `#interior`

- 2カラム段違い: 左 `interior-01`（3:4縦）、右カラムは `margin-top: 96px` で下げ、`interior-02`（4:3横）＋短文
- 860px以下で1カラム化し段差解除

### 4.7 ふじたの一日 `#day`

- 写真を使わない4項目のタイムライン。`grid 4列`・罫線1pxのセル分割（背景に `--line`、セルに `--sumi`）
- 各セル: 時刻（朱soft・sans）→ 小見出し（明朝17px）→ 説明（13px）
- 内容: 5:00 豊洲で目利き／14:00 出汁と仕込み／17:30 暖簾がかかる／20:00 今日の一皿

### 4.8 店舗情報 `#info`

- 左: 定義テーブル（th 8em・sans小・下罫線）。項目: 店名／住所／アクセス／電話（`tel:`リンク）／営業時間（確認前は「店舗にご確認ください」）／席数
- 右: 外観写真枠（16:10・任意）。写真が用意できない場合はGoogleマップ埋め込みに差し替え（実装指示書§2.3）

### 4.9 ご予約 `#reserve`

- 中央揃え。電話発信（`.btn.primary`・`tel:0364512969`）、食べログ・一休（`.btn`・別タブ・`noopener`）
- 注記「※現在は外部予約サイトへご案内しています。自社予約システムは今後導入予定です。」を必ず表示

### 4.10 フッター

屋号／アンカーナビ＋INSTAGRAM（URL確定まで `#`）／コピーライト。

---

## 5. モーション設計一覧

| 対象 | 種類 | 時間 | イージング | 遅延 | reduced-motion時 |
|---|---|---|---|---|---|
| ローダー屋号 | フェードイン＋上移動 | 1.1s | ease | 0.15s | ローダー自体を非表示 |
| ローダー解除 | フェードアウト | 0.9s | ease | load後1.1s（上限2.6s） | 即時解除 |
| ヒーロー写真 | ケンバーンズ（scale 1.02→1.12） | 24s | ease-out | `.ready`後 | 無効（静止） |
| ヒーロータイトル | 1文字ずつ出現 | 各1s | ease | 0.15s＋0.12s×n | 全文字即時表示 |
| ヒーローサブ | フェードイン | 1.4s | ease | 0.9s | 即時表示 |
| 節気印 | フェード＋上移動 | 1s | ease | 1.3s | 即時表示 |
| SCROLLキュー | 縦線の伸縮ループ | 2.2s | ease-in-out | - | 停止 |
| `.reveal` 全般 | フェード＋24px上移動 | 1s | ease | 交差時 | 即時表示 |
| 料理カード写真 | ホバーで scale 1.04 | 1.2s | ease | - | 無効 |
| ヘッダー変化 | 背景・余白 | 0.4s | ease | - | 維持（機能的変化のため） |

---

## 6. ロジック設計（JavaScript）

依存ライブラリなし（Vanilla JS）。`<script>` 1ブロック・即時関数内で完結。

### 6.1 二十四節気判定

- 近似日付テーブル `SEKKI`（月, 日, 節気名, 読み）全24件を保持
  - 小寒1/5, 大寒1/20, 立春2/4, 雨水2/19, 啓蟄3/5, 春分3/20, 清明4/4, 穀雨4/20, 立夏5/5, 小満5/21, 芒種6/5, 夏至6/21, 小暑7/7, 大暑7/22, 立秋8/7, 処暑8/23, 白露9/7, 秋分9/23, 寒露10/8, 霜降10/23, 立冬11/7, 小雪11/22, 大雪12/7, 冬至12/21
- 判定: テーブルを先頭から走査し、「当日 ≧ 開始日」を満たす最後の要素を採用。1/1〜1/4は該当なし→初期値「冬至」を採用
- 精度: 実際の節入り日と最大±1日の誤差を許容（要件§7-4）
- 出力先: `#sekkiSeal`（節気名）／`#sekkiYomi`（読み）

### 6.2 漢数字日付

- 変換関数 `kd(n)`: 1〜31対応（十の位が1なら「十」、2以上なら「二十」等＋一の位）
- 出力形式: `八月四日（火）`。曜日は `["日","月","火","水","木","金","土"]`
- 出力先: `#today`

### 6.3 ヘッダースクロール

`scroll` イベント（`passive: true`）で `window.scrollY > 40` を判定し `.scrolled` をトグル。

### 6.4 出現演出

`IntersectionObserver`（`threshold: 0.15`）で `.reveal` を監視。交差時に `.on` を付与し `unobserve`（1回限り）。

### 6.5 ローダー制御・文字分割

- `prefers-reduced-motion` 判定を最初に1回取得し、全演出の分岐に使用
- 通常時: `window.load` から1100ms後に解除。保険として `setTimeout 2600ms` でも解除（多重呼び出しはフラグで無害化）
- 解除時に `#hero.ready` 付与＋タイトル文字分割（`textContent` を1文字ずつ `span.ch` 化し `animation-delay` を設定）

### 6.6 画像フォールバック

各 `<img>` の `onerror` 属性で `closest` の写真枠に `.missing` を付与し、`<img>` 自身を除去（インライン記述・JS本体に依存しない）。

---

## 7. レスポンシブ設計

| 幅 | 変化点 |
|---|---|
| 〜860px | ナビリンク非表示（「ご予約」のみ残す）／セクション余白 `80px 6vw`／想い・店舗情報・店内を1カラム化（段違い解除）／タイムラインを2×2に |
| 〜520px | タイムラインを1カラムに |

ヒーローの縦書きタイトルはスマホでも縦書きのまま（`clamp` で34pxまで縮小）。

---

## 8. アクセシビリティ設計

- 全 `<img>` に内容を表す `alt`
- フォーカス可視化: `:focus-visible` に朱softの2pxアウトライン＋3pxオフセット
- 装飾要素（ローダー・湯気等）は `aria-hidden="true"`、アイコン的リンクは `aria-label`
- `prefers-reduced-motion: reduce` で全演出を無効化（§5参照）
- ナビゲーションは `nav` 要素＋`aria-label` で区別

---

## 9. ディレクトリ構成（フェーズ1）

```
/
├─ index.html          … fujita-prototype-v2.html をリネーム・本番化したもの
├─ images/
│   ├─ hero.jpg        … 後日配置（未配置でも動作する）
│   ├─ dish-01.jpg
│   ├─ dish-02.jpg
│   ├─ dish-03.jpg
│   ├─ interior-01.jpg
│   ├─ interior-02.jpg
│   └─ interior-03.jpg … 任意
├─ favicon.svg
├─ ogp.jpg             … OGP用（写真到着後に作成）
├─ robots.txt
└─ sitemap.xml
```

フェーズ2（Astro化）のコンポーネント分割は実装指示書§3を参照。
